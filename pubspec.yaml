name: native_storage_utility
description: "WIP - Some storage utilities using FFI/JNI"
version: 0.0.1
homepage: https://github.com/felipheallef/native_storage_utility

environment:
  sdk: ^3.12.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.2.0
  jni: ^1.0.0
  jni_flutter: ^1.0.1
  objective_c: ^9.4.1
  plugin_platform_interface: ^2.1.8
  win32: ^5.15.0
  xdg_desktop_portal: ^0.1.14

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  args: ^2.7.0
  ffigen: ^20.1.1
  jnigen: ^0.16.0
  lints: ^6.0.0
  test: ^1.25.6

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: dev.felipheallef.native_storage_utility
        pluginClass: NativeStorageUtilityPlugin
      ios:
        pluginClass: NativeStorageUtilityPlugin
      linux:
        pluginClass: NativeStorageUtilityPlugin
      macos:
        pluginClass: NativeStorageUtilityPlugin
      windows:
        pluginClass: NativeStorageUtilityPluginCApi